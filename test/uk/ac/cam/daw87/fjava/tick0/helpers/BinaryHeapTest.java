package uk.ac.cam.daw87.fjava.tick0.helpers;

public class BinaryHeapTest {
    private static int[] test = {1, 2};

    public static void main(String[] args) {
        /*
        BinaryHeap heap = new BinaryHeap(test.length);
        for (int i: test){
            heap.insert(i, 0);
        }
        //System.out.println(heap);
        while (!heap.isEmpty()){
            System.out.println(heap);
            System.out.println(heap.getMin()[0]);
        }
        System.out.println(heap);
        */
    }
}
